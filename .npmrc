# Tell pnpm to include Linux/musl binaries in the lockfile even when
# installing on macOS. Without this, @img/sharp-linux-musl-x64 and similar
# native packages are absent from pnpm-lock.yaml and fail to load in Docker
# (Alpine uses musl libc).
# See also: package.json pnpm.supportedArchitectures
