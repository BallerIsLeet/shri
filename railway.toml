# Project-level service registry.
# Each service picks up its own build/deploy config from apps/{name}/railway.toml.
# Docker build context is always the repo root, so Dockerfiles can COPY
# files from anywhere in the monorepo.

[[services]]
name = "web"
source = "apps/web"

[[services]]
name = "worker"
source = "apps/worker"
